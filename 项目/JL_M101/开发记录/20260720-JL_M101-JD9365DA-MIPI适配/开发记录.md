---
project: JL_M101
kind: log
date: 2026-07-20
---

# JL-M101 JD9365DA MIPI 适配 — 开发记录

> **状态更新（2026-07-22）：实机已完整出图。**  
> 定稿笔记（板件新建 + 显示开发）请读：  
> [../20260722-JL_M101-板件新建与MIPI显示定稿/README.md](../20260722-JL_M101-板件新建与MIPI显示定稿/README.md)  
> 本文保留 phase1 落地与 07-20～21 调试流水，便于对照补丁。

| 项 | 内容 |
|---|---|
| 日期 | 2026-07-20（实机调试至 2026-07-21；**出图定稿 2026-07-22**） |
| 板型 | **`p1_nor_JL_M101`** |
| 参照板型 | **`p1_nor_8733`**（显示链路 + RTL8733BU；DTS 原软链 `p1_nor`） |
| 屏型号 | JL-M101B196-P21WX-M402632 |
| 驱动 IC | JD9365DA-H3 |
| 分辨率 / 接口 | 800×1280，MIPI DSI **4-lane**，video mode |
| panel 名 | `jl_m101_jd9365da` |
| 合并补丁 | [patches/jl-m101-mipi-phase1.patch](./patches/jl-m101-mipi-phase1.patch) |
| 板级档案 | [board-profile-p1_nor_JL_M101.md](../../../../skills/h133-display/board-profile-p1_nor_JL_M101.md) |
| 定稿文档 | [20260722-…定稿](../20260722-JL_M101-板件新建与MIPI显示定稿/README.md) |
| 索引 | [../README.md](../README.md) |

---

## 0. 当前结论（先看这个）

**从 `p1_nor_8733` 克隆独立板级，只换 MIPI panel（京龙 init），UI/WiFi/视频旋转保持 8733 不变。**  
禁止改共享的 `p1_nor` / `p1_nor_8733` DTS 迁就本屏。

```bash
# 应用补丁（干净树）
bash H133-AI-Skills/开发记录/板件开发记录/JL_M101/20260720-JL_M101-JD9365DA-MIPI适配/patches/apply-jl-m101-mipi.sh

# 编译（与 JYY070 脚本同结构）
./tools/build_p1_nor_JL_M101.sh kernel   # 先验证 panel
./tools/build_p1_nor_JL_M101.sh full     # 完整镜像
# 产物：out/h133_linux_p1_nor_JL_M101_uart0_nor.img
```

**实机进度（定稿）：** WiFi 8733bu 正常；内核 `panel_init` + PWM0@50kHz 背光；转接板 **3.3V-RESET**；**LCD 已完整出图**。  
历史卡点（已闭环）：U-Boot smooth、DSI 读探针、USB 占 PB11/PB12、RESET 分压档、PB12 须为 PWM0 而非 `lcd_bl_en`。详见 [定稿 04-踩坑](../20260722-JL_M101-板件新建与MIPI显示定稿/04-实机调试与踩坑.md)。

**Panel 编入方式（与工程一致）：** `Kconfig` 中 `CONFIG_LCD_SUPPORT_JL_M101_JD9365DA` **`default y`**（同 JYY070）；多 panel 可同时编进内核，运行时靠 DTS `lcd_driver_name` 选择。**不需要**独立 fragment。

| 项 | `p1_nor_8733` | **本板 `p1_nor_JL_M101`** |
|---|---|---|
| 屏驱动 | `default_lcd`（无 JD9365 init） | **`jl_m101_jd9365da`**（京龙 init） |
| 分辨率 | 800×1280 MIPI | **同** |
| DTS | 软链 `p1_nor` | **独立** `linux-5.4/board.dts` |
| UI `disp_rotate` | 3 | **3** |
| 视频 `VIDEO_WB` | 90 | **90** |
| WiFi | RTL8733BU | **同**（不开 AIC8800） |
| 背光 | （旧）`lcd_bl_en` GPIO | **PWM0@PB12 / 50kHz** |

---

## 1. 背景与目标

### 1.1 为何对齐 8733 而不是裸 p1_nor / JYY070

| 参照 | 适用点 | 不直接当基线的原因 |
|---|---|---|
| **`p1_nor_8733`** | 同 MIPI 竖屏 UI、异显、**8733BU WiFi** | —（**选定基线**） |
| `p1_nor` | 显示链路同源 | WiFi 为 AIC8800，与目标板不符 |
| `p1_nor_JYY070` | 板件隔绝 / 分阶段文档可抄 | LVDS 横屏 + `disp_rotate=0`，显示框架不同 |

### 1.2 硬件要点（规格书 + init + 主板）

| 项 | 值 | 来源 |
|---|---|---|
| 模组 | JL-M101B196-P21WX-M402632 | 规格书 |
| IC | JD9365DA-H3 | 规格书 / 模组说明 |
| 分辨率 | 800(H)×1280(V) | 规格书 |
| 接口 | MIPI 4-lane（D0–D3 + CLK） | 规格书 Pin |
| Reset 时序 | H 5ms → L 10ms → H 120ms，再发码 | init txt |
| Porch（vendor） | HSA20 HBP20 HFP40 / VSA4 VBP20 VFP20 | init 头 |
| Allwinner 换算 | `hspw=20 hbp=40 ht=880` / `vspw=4 vbp=24 vt=1324` | 与现 p1_nor 注释时序一致 |
| RST / PWM | `PB11 = LCD-RST` / `PB12 = LCD-PWM(=PWM0)` | SoC GPIO 页 + `s10` LCM 页 |
| 背光 PWM | `LCD-PWM → PT4117B EN`，**50kHz** | `s10-h133-v1_0625.pdf`：`PWM_Frequence=50KHz` |
| 转接板 RESET | `LCD-RST → R52 → LCD_RST → CON5 pin6 RESET`；**R52/R53 选 3.3V 或 1.8V 分压** | `s10-h133-v1_0625.pdf`：`3.3V-RESET: R52=0R,R53=NC` / `1.8V-RESET: R52=1.5K,R53=1.8K` |
| RESET 输入高 | VIH ≥ 0.7×IOVCC（VCC=3.3V 时约 **2.31V**） | JL 规格书 |
| LCD 供电使能 | `LCD-EN` | 第 14/15 页；控制 LCD 偏压/模拟电源，不是背光 LED 电源 |
| 背光 LED | `VLED+ / VLED-` | 第 14 页；外部背光接口，规格书典型约 12V/160mA |
| 主板显示历史 | **JYY070 为 LVDS**，非 MIPI 参考 | 本主板 MIPI 走 PD 组 + 转接板 CON5，**未在实机验证过 DSI 出图** |
| `0x80=0x03` | 四 lane | init |

**关键差：** 8733 共用的 `default_lcd` **不发** JD9365 init；京龙屏必须独立 panel 灌码。`PB12` 不是普通 `lcd_bl_en` GPIO，而是 `LCD-PWM`，不能继续当背光 enable GPIO 使用。

树内资料：

- `H133-AI-Skills/硬件资料/四川京龙/JL-M101B196-P21WX-M402632 规格书V1.0.pdf`
- `…/C-JD9365DA-H3_…_800x1280_…20210830 (1)(1).txt`
- `H133-AI-Skills/硬件资料/TP-H10_H133 V2.0 2026.6.12.pdf`
- `H133-AI-Skills/硬件资料/四川京龙/s10-h133-v1_0625.pdf`（主板↔屏 **40P 转接板**，含 R52/R53 RESET 档）
- TP（后续）：`GSL3670_DataSheet_Chinese_RevA2.2.pdf`

---

## 2. 设计原则

1. **板件隔绝**：新板 `p1_nor_JL_M101`；只改本板 `configs/` 与 `h133-p1_nor_JL_M101/`。  
2. **一屏一驱动**：`jl_m101_jd9365da.c`，不改 `default_panel.c`。  
3. **拆软链**：本板 `linux-5.4/` 为实文件（从 `p1_nor` 拷贝后再改），避免动 8733。  
4. **App 先不动**：`disp_rotate=3`、`VIDEO_WB_MIRROR_ROTATE=90`，复用 8733 竖屏路径。  
5. **最小公共改动**：仅 panel 注册、兄弟板 Kconfig/defconfig 的 `not set`、libtmedia 增加本 target。

---

## 3. 补丁分段

| 段 | 文件 | 内容 |
|---|---|---|
| **01** | [01-kernel-panel.patch](./patches/01-kernel-panel.patch) | `jl_m101_jd9365da.c/.h`；`Kconfig` / `Makefile` / `panels.c` / `panels.h` |
| **02** | [02-board-tree.patch](./patches/02-board-tree.patch) | `device/.../p1_nor_JL_M101/`（无 bootlogo）；`openwrt/.../h133-p1_nor_JL_M101/`；`tools/build_p1_nor_JL_M101.sh` |
| **03** | [03-shared-hooks.patch](./patches/03-shared-hooks.patch) | libtmedia `VIDEO_WB=90` 含本板；`p1_nor`/`8733`/`JYY070` defconfig 与 kernel config 的 JL not-set |
| **合并** | [jl-m101-mipi-phase1.patch](./patches/jl-m101-mipi-phase1.patch) | 01+02+03 |

```bash
bash patches/apply-jl-m101-mipi.sh          # 打合并补丁
bash patches/apply-jl-m101-mipi.sh --check
bash patches/apply-jl-m101-mipi.sh 01       # 只打 kernel panel
bash patches/gen-jl-m101-mipi-patch.sh      # 从当前树重生成
```

`bootlogo.bmp`：不进补丁；apply 时若缺失则从 `p1_nor_8733` 复制。

---

## 4. 变更文件明细

### 4.1 Kernel panel（01）

| 文件 | 说明 |
|---|---|
| `kernel/.../lcd/jl_m101_jd9365da.c` | Reset 序列 + 京龙 init 全表 `dsi_gen_wr`；**无 DSI 读探针**；调通信阶段 **不拉背光**（`LCD_bl_*` 已从 open_flow 去掉） |
| `brandy/.../jl_m101_jd9365da.c` | 与内核同 init 策略；U-Boot 侧同样去掉背光 |
| `brandy/.../bootGUI/boot_gui.c` | `disp_init_enable=0` 时 skip，避免 smooth |
| `kernel/.../lcd/jl_m101_jd9365da.h` | 导出 `jl_m101_jd9365da_panel` |
| `kernel/.../lcd/Kconfig` | `CONFIG_LCD_SUPPORT_JL_M101_JD9365DA` |
| `kernel/.../disp/Makefile` | 编译 `jl_m101_jd9365da.o` |
| `kernel/.../lcd/panels.c` / `panels.h` | 注册到 `panel_array` |

**open_flow：** power_on（含 Reset：H 5ms → L 10ms → H 120ms）→ panel_init → tcon_enable；（调屏阶段无 bl_open）。  
**init 表：** 由京龙 txt 机械转换；`REGFLAG_DELAY` / `REGFLAG_END_OF_TABLE` 按 **cmd** 字段判断（与 vendor `{REGFLAG_DELAY, ms, {}}` 一致）。

### 4.2 板级树（02）

| 路径 | 说明 |
|---|---|
| `device/config/chips/h133/configs/p1_nor_JL_M101/` | 从 8733 克隆；`linux-5.4` **实目录**（非软链） |
| `…/linux-5.4/board.dts` | `lcd_driver_name = "jl_m101_jd9365da"`；时序见上表；`PB11=LCD-RST`，`PB12=LCD-PWM` |
| `…/linux-5.4/config-5.4` | `CONFIG_LCD_SUPPORT_JL_M101_JD9365DA=y`；JYY070 LVDS 关闭 |
| `openwrt/target/h133/h133-p1_nor_JL_M101/` | lunch `h133-p1_nor_JL_M101-tina`；hostname `TinaLinux-JL_M101`；继承 8733 包选型（含 `rtl8733bu-kmod`） |
| `…/busybox-init-base-files/etc/setting.ini` | `disp_rotate=3`，`tp_rotate=0`（随 8733） |
| `tools/build_p1_nor_JL_M101.sh` | 对齐 JYY070 编译脚本：`autoconfig` + kernel/bootloader + `m` + `p` |

### 4.3 公共挂钩（03）

| 文件 | 说明 |
|---|---|
| `openwrt/.../libtmedia/Makefile` | `CONFIG_TARGET_h133_p1_nor_JL_M101` → `VIDEO_WB_MIRROR_ROTATE=90` |
| `openwrt/target/h133/h133-p1_nor{,_8733,_JYY070}/defconfig` | `# CONFIG_TARGET_h133_p1_nor_JL_M101 is not set` |
| `device/.../p1_nor/.../config-5.4`、`p1_nor_JYY070/.../config-5.4` | `# CONFIG_LCD_SUPPORT_JL_M101_JD9365DA is not set` |

---

## 5. DTS lcd0 要点（本板，已按 s10 原理图对齐）

下面是按当前原理图结论应采用的方向；实际 DTS 仍需逐项核对 `lcd_pwm_ch` / `LCD-EN` 绑定，不能只把 `PB12` 当普通 GPIO 拉高。

```dts
lcd_driver_name     = "jl_m101_jd9365da";
lcd_if              = <4>;          /* DSI */
lcd_x / lcd_y       = <800> / <1280>;
lcd_dclk_freq       = <70>;         /* 实机可再调；vendor PLL_CLOCK=420 为 MTK 口径 */
lcd_hbp/ht/hspw     = <40>/<880>/<20>;
lcd_vbp/vt/vspw     = <24>/<1324>/<4>;
lcd_dsi_lane        = <4>;
lcd_gpio_0          = <&pio PB 11 …>;  /* RESET */
/* s10 原理图：PB12=LCD-PWM → PT4117B EN，禁止当普通 lcd_bl_en GPIO */
lcd_pwm_used        = <1>;
lcd_pwm_ch          = <0>;             /* PB12 复用 PWM0（mux 0x3） */
lcd_pwm_freq        = <50000>;         /* 原理图标注 PWM_Frequence=50KHz（非 500Hz） */
```

备注：

- 2026-07-22：已按 `s10-h133-v1_0625` + 京龙 init 对齐——开 `pwm0@PB12`、`LCD_bl_open`；init/porch 本就与 vendor txt 一致。
- `VLED+ / VLED-` 是屏背光 LED 供电输出/接口；板载由 PT4117B 从 5V 升压驱动。
- `LCD-PWM` 进 PT4117B 的 EN；勿把 PB12 配成 `lcd_bl_en` 纯 GPIO。
- `usbc0` 里曾残留 `usb_id_gpio=PB11`、`usb_det_vbus_gpio=PB12`，会和 LCD RST/PWM 冲突，已从本板 DTS 移除。

---

## 6. 编译与验收

### 6.1 编译

```bash
./tools/build_p1_nor_JL_M101.sh kernel
./tools/build_p1_nor_JL_M101.sh full
# 或 lunch 后：./build.sh autoconfig -o openwrt -i h133 -b p1_nor_JL_M101 -n nor
```

注意：改 panel 后需重编内核；`m` 前建议已有 Tina `out/kernel/build`（8733BU 包依赖 `Module.symvers`）。

### 6.2 验收清单

- [ ] 内核编过，无 `jl_m101_jd9365da` 相关 link/compile error  
- [ ] `dmesg` 可见 panel / DSI / lcd 打开  
- [ ] 背光亮；无花屏 / 闪屏；分辨率 800×1280  
- [ ] desk：`disp_rotate=3`，方向与 8733 一致  
- [ ] WiFi 仍 `rtl8733bu`，bsp **未**开 AIC8800  
- [ ] 串编回归：`p1_nor_8733` / `p1_nor_JYY070` DTS 未被本改动改坏  

### 6.3 常见问题

| 现象 | 优先查 |
|---|---|
| 背光亮但无图 | 若已有 `panel_init` + `attached ok`：查 **转接板 RESET 分压（R52/R53）**、RESET 波形、MIPI CLK/HS；勿对 RESET 外部恒压灌 3.3V |
| `panel_init` 后无后续 DISP log | 曾由 **DSI LP 读探针** 引起 DPHY 卡读态 + `dsi_gen_wr` 死等；已删探针 |
| `dclk` 配置 70 但 log 显示 `dclk real 105`、`dsi_rate 150` | H133 BSP `clk_tbl` + `dsi_div=bitwidth/lane` **正常**，非本板独有配错 |
| 完全不亮 | 背光外部电源/恒流源、`LCD-PWM`、`LCD-EN`、内核 `lcd_used`、panel 名是否匹配 |
| `8733bu: Unknown symbol mutex_destroy` | 内核 `.config` 变过 / 清过 `.config` 后未重编 ko：`./tools/build_p1_nor_JL_M101.sh rootfs` |
| 打补丁失败 | 是否已存在板级目录；用 `--check`；或分段打 01→02→03 |
| WiFi Oops | 先 `kernel` 再 `m`（与 JYY070 8733BU 记录相同） |

---

## 7. 未做（后续）

| 项 | 说明 |
|---|---|
| U-Boot logo | 目前 `boot_gui_init` 按 `disp_init_enable=0` 跳过，避免内核 smooth 跳过灌码；调屏完成后再决定是否恢复 U-Boot logo |
| GSL3670 触摸 | 资料已有；现仍沿用 8733 GT911 配置 |
| 实机微调 | RESET 硬件档确认后：`LCD-PWM`、`LCD-EN`、MIPI 示波器、gamma |
| 背光软件 | **已恢复**（2026-07-22）：`lcd_pwm_ch=0` / 50kHz / `LCD_bl_open` |
| 双板串编脚本 | 可仿 `verify_build_8733_then_JYY070.sh` 加 JL_M101 |

---

## 8. 与 JYY070 显示文档的关系

| 本目录 | JYY070 LVDS 文档 |
|---|---|
| 板件隔绝、分阶段补丁、board-profile | 流程模板可抄 |
| MIPI + JD9365 init | 对应 JYY070 的「LVDS 时序 panel」，接口不同 |
| UI 对齐 **8733（rot=3）** | JYY070 对齐的是「异显框架 + rot=0」 |

---

## 9. 2026-07-20 实机调试流水

### 9.1 初始现象

- 现象：背光亮，但 LCD 无图；HDMI 有图；烧回 `p1_nor_8733` + 原配屏可出图。
- 结论：主系统、DE/HDMI、rootfs/UI 不是首要问题；问题集中在 JL_M101 的 LCD MIPI/panel/电源控制。

### 9.2 U-Boot smooth display 问题

| 现象 / log | 判断 | 处理 |
|---|---|---|
| U-Boot 出现 `boot_gui_init:start`、`jl_m101_jd9365da: panel_init`，内核出现 `smooth display screen:0` | U-Boot 已开屏并写 `boot_disp`，内核 smooth 复用状态，跳过内核 `cfg_open_flow` | `boot_gui.c` 读取 `disp_init_enable`，为 0 时直接 `boot_gui_init:skip (disp_init_enable=0)`；`uboot-board.dts` 设 `disp_init_enable=<0>` |
| 新 log 出现 `boot_gui_init:skip`，内核不再 `smooth display`，并出现 `jl_m101_jd9365da: panel_init` | smooth 问题排除，内核确实开始灌 JD9365 init | 进入 MIPI/panel 层排查 |

构建注意：一次 `uboot_force` 触发 OpenWrt rootfs 目标失败，但日志显示 `boot_gui.o` 和 `u-boot.bin` 已重新生成；后续用 `./build.sh pack` 出包。另一次 U-Boot 增量编译遇到旧 `.o` 截断，使用 `make clean` 清理 U-Boot 后恢复。

### 9.3 init 写法试探

| 改动 | 镜像 md5 | 结果 |
|---|---|---|
| 去掉额外 soft reset，保留 `0x11/0x29` 特判为 DCS 0 参数 | `d0d7f6a699bcd71ce2a1cef0c45a24f6` / 后续修正包 `cb7a5b1dd4aef881850edc2750666c91` | U-Boot skip 生效，内核执行 panel_init，但 LCD 仍无图 |
| 全表按同系 `jd9366ab_3` 方式走 `dsi_gen_wr()`，不再特判 `0x11/0x29` | `46f80d6df6d17389b058c556a18ac8bf` | 现象不变 |

结论：单纯在 GEN/DCS 之间切 `0x11/0x29` 没有命中根因。

### 9.4 DSI 读探针

临时在 `kernel/.../jl_m101_jd9365da.c` 里加入 probe：

- `sunxi_lcd_dsi_dcs_read(sel, 0x0A, ...)`
- `sunxi_lcd_dsi_dcs_read(sel, 0xDA, ...)`

诊断包：

| 镜像 md5 | 关键 log | 判断 |
|---|---|---|
| `eaa4edf2d33ba6414600f8774d7eacad` | `probe pre 0x0A ret=0 num=0 val=00 00 00 00`，`probe pre 0xDA ret=0 num=0 val=00 00 00 00` | 屏当前没有回读命令；继续盲改 init 表意义不大，应查 RESET/电源/MIPI LP 命令通道 |

注意：曾只看到 `pre` probe、无 `post`，根因见 **§9.6**（探针已删）。

### 9.5 GPIO / 原理图核对

原理图 `TP-H10_H133 V2.0 2026.6.12.pdf`：

| 网络 | H133 管脚 / 说明 | 结论 |
|---|---|---|
| `LCD-RST` | `PB11` | panel reset 脚 |
| `LCD-PWM` | `PB12` | 背光升压芯片 EN/调光输入，原理图标注 `PWM_Frequency=500Hz` |
| `LCD-EN` | 第 14/15 页电源使能 | LCD 偏压/模拟电源使能，不是背光 LED 电源 |
| `VLED+ / VLED-` | 背光接口 | 规格书典型约 12V/160mA；可用外部恒流背光源临时验证亮度 |

曾发现本板 DTS `usbc0` 残留：

```dts
usb_id_gpio = <&pio PB 11 GPIO_ACTIVE_HIGH>;
usb_det_vbus_gpio = <&pio PB 12 GPIO_ACTIVE_HIGH>;
```

这与 `LCD-RST/LCD-PWM` 冲突，已从 `p1_nor_JL_M101` 的 `board.dts` / `linux-5.4/board.dts` 移除。修正包：

| 镜像 md5 | 内容 |
|---|---|
| `c5132015ca06bfc4ff4ba35b5aeeb7db` | 移除 USB 对 PB11/PB12 的占用，保留 probe 日志 |

### 9.6 DSI 读探针导致 panel_init 挂死（已修复）

| 现象 | 根因 | 处理 |
|---|---|---|
| log 仅有 `probe pre 0x0A/0xDA num=0`，无 `post`、后续 DISP 停 | LP 读触发 BTA，屏不回读时 DPHY 卡在读态；后续 `dsi_gen_wr` 内 `while(dsi_inst_busy)` **无超时** | 删除 `LCD_panel_probe` 及全部 `sunxi_lcd_dsi_dcs_read` |

修复后典型 log：`jl_m101_jd9365da: panel_init` → `attached ok, mgr0<-->dev0` → `type:1,mode:0`（video）。

### 9.7 调通信阶段：背光与 DTS

- 曾临时：外部电源供 `VLED+` + `lcd_pwm_used=0`，open_flow 不含 `LCD_bl_open`。
- **2026-07-22 已恢复板载背光软件路径**（见 §5）：`pwm0@PB12` / 50kHz / `LCD_bl_open`。
- `usbc0` 对 PB11/PB12 的占用保持注释掉。

### 9.8 屏侧电压与转接板 RESET（2026-07-21）

| 测量点（用户：屏 FPC 侧） | 值 | 含义 |
|---|---|---|
| VCC | 3.3V | 数字供电正常 |
| RESET | 1.8V（静态） | 低于 VIH≈2.31V（按 3.3V IO 计）时，屏可能**无法可靠退出复位/识别高电平** |

转接板 `s10-h133-v1_0625.pdf`（CON5 40P 座旁）：

- **R52**（0402）：串在 `LCD-RST`（主板）与 `LCD_RST`（座子 pin5/6 链路）之间；丝印 **R52**，靠 CON5 **LCD-RST / RESET** 网络。
- **R53**（0402）：分压到 GND，标 **1.8K/NC**。
- 注释：`3.3V-RESET: R52=0R,R53=NC`；`1.8V-RESET: R52=1.5K,R53=1.8K`。

**必做区分（改电阻前）：** 万用表量 **R52 靠主板 `LCD-RST` 输入端**对地：

| R52 输入端电压 | 结论 | 动作 |
|---|---|---|
| **3.3V** | 当前为 1.8V **分压档** | 改 R52=0R、R53=NC，撤外部灌 RESET，用现镜像重试 |
| **1.8V** | SoC PB 域或上游即 1.8V | 换 R52/R53 **无效**，需电平转换或确认屏 IOVCC/逻辑电平 |

**无效操作：** 对 RESET **外部恒压 3.3V** — 驱动需 **H→L 10ms→H** 硬件复位脉冲，外部强灌高电平会**与 PB11 对打**，脉冲发不出去，**不能**代替改 R52/R53。

### 9.9 `lcd_dsi_if` burst 试验（已否定）

| 配置 | 结果 |
|---|---|
| `lcd_dsi_if=<2>`（video burst），kernel + uboot DTS 同步 | 与 video 相同：`panel_init` 完成、`dclk real 105` / `dsi_rate 150`，**仍无图** |
| 已恢复 | `lcd_dsi_if=<0>`（video mode） |

### 9.10 当前待验证 / 下一步

1. **硬件（最高优先级）：** 按 9.8 量 R52 输入端 → 必要时改 **3.3V-RESET** 档；勿外部灌 RESET。
2. **若改档仍无图：** 示波器 CON5 **MIPI-CKP/N（pin14/15）** 是否有 HS 差分；无 HS → PHY/转接/lane；有 HS → init/屏体/极性。
3. **软件：** 保持 `disp_init_enable=0`、无 DSI 读探针；背光已按原理图恢复 PWM0（仍须硬件 RESET 档正确）。
4. **触摸：** GT911 I2C 失败为转接/屏线独立问题，与 LCD 无图可并行排。
5. **镜像参考：** burst 试验打包 md5 `274a8e064e9dee08c496c3f897d0cb48`（已改回 video，重 pack 即可）。

---

*2026-07-20：phase1 落地；实机 MIPI 排查。*  
*2026-07-21：软件管线确认完成；卡点收敛至转接板 RESET 电平/脉冲与 MIPI 物理层。*  
*2026-07-22：按 s10 原理图 + 京龙 init 对齐背光（PWM0@PB12 / 50kHz / bl_open）；init 表无需改。*  
*2026-07-22：实机完整出图；定稿笔记见 [20260722-JL_M101-板件新建与MIPI显示定稿](../20260722-JL_M101-板件新建与MIPI显示定稿/README.md)。*
