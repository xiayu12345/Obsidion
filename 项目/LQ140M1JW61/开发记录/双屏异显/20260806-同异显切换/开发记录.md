---
project: LQ140M1JW61
kind: log
date: 2026-08-06
---

# 同显 / 异显切换 — 开发记录

| 项 | 内容 |
|---|---|
| 日期 | 2026-08-06 |
| 范围 | **架构级**（`libuapi` / disp / tplayer，非单板专属） |
| 验证板 | `p1_nor_JL_M101`（首验） |
| 状态 | **已定稿（WB 常开 + HDMI 层显隐）** |
| 补丁 | [patches/](./patches/) |
| 前置 | [20260805-HDMI异显最小验证](../20260805-HDMI异显最小验证/)（TEMP 关 WB **已废弃**） |
| 架构参考 | [LV_DS-双屏异显中间件架构.md](../LV_DS-双屏异显中间件架构.md) |

---

## 1. 产品规则

| 项 | 决策 |
|---|---|
| 默认 | **同显 SAME** |
| WB 管道 | **常开**（启动/HPD `enable` 一次）；**禁止**为切模式拆建 WB |
| 切模式 | 只改 HDMI 上 WB 层显隐 + 视频 `disp_screen` |
| 播中切模式 | **不允许**（`-EBUSY`） |
| HDMI 未插 | 视频强制 LCD |
| SAME | HDMI=WB 镜像；视频 DE0；`hdmi_out=1` |
| DIFF | HDMI 上 WB 层 `enable=0`；视频 DE1；`hdmi_out=0` |

---

## 2. 架构（最终）

```text
启动/HPD → disp_dual_display_enable()     ← 只做一次，管道常驻
                │
disp_mode same  → SET_HDMI_OUT=1 + 视频下次播 screen=1
disp_mode diff  → SET_HDMI_OUT=0 + 视频下次播 screen=2
```

**已废弃：**

- 用户态 disable / 模式里 `DISP_DUAL_DISPLAY_DISABLE`
- `g_dual_display_prefer_enable`
- [20260805 TEMP 补丁](../20260805-HDMI异显最小验证/patches/)（编译期关 WB）

`DISP_DUAL_DISPLAY_DISABLE` 占号 **0x408**，内核直接 `-EINVAL`。

---

## 3. 补丁清单

| 文件 | 内容 |
|---|---|
| `01-kernel-dual-hdmi-out.patch` | `sunxi_display2.h` + `dev_disp` / `dev_fb` |
| `02-libuapi-disp-mode.patch` | `disp_dual` + `disp_mode` CLI |
| `03-tlayer-media-session.patch` | 建层选屏 + fence + boot_sync |
| `disp-mode-same-diff.patch` | 合并包 |
| `apply-disp-mode.sh` | 一键 apply |

```bash
bash H133-AI-Skills/开发记录/双屏异显/20260806-同异显切换/patches/apply-disp-mode.sh
```

---

## 4. 内核 ioctl

| cmd | 值 | 用途 |
|---|---|---|
| `DISP_DUAL_DISPLAY_ENABLE` | 0x407 | 确保 WB 起来（幂等） |
| `DISP_DUAL_DISPLAY_DISABLE` | 0x408 | **废弃** → `-EINVAL` |
| `DISP_DUAL_DISPLAY_GET_EN` | 0x409 | WB 是否已 enable |
| `DISP_FB_CLONE_ALLOW_*` | 0x40a/40b | pan clone，默认关 |
| `DISP_DUAL_DISPLAY_SET_HDMI_OUT` | 0x40c | 关/开 HDMI 上 WB 层 |
| `DISP_DUAL_DISPLAY_GET_HDMI_OUT` | 0x40d | 当前 hdmi_out |

### 踩坑（历史）

曾用 DISABLE 拆 WB → `scheduling while atomic`（unregister 未 unlock）。产品路径只走 `SET_HDMI_OUT`。

---

## 5. 用户态 / 机上命令

```bash
disp_mode same|diff|get
```

`sunxi_disp_mode_apply`：`fb_clone=0` → `ensure_wb_up` → `set_hdmi_output`。

---

## 6. 编译 / 验收

按目标板编 kernel + rootfs。首验：`./tools/build_p1_nor_JL_M101.sh full`。

| 步骤 | 期望 |
|---|---|
| 冷启 | WB 开，同显 |
| `disp_mode diff` 再播（HDMI 已插） | LCD 仅 UI，视频只 HDMI |
| `disp_mode same` 再播 | 同显恢复 |
| 播中切换 | EBUSY |

---

## 7. 变更文件

```text
kernel/linux-5.4/include/video/sunxi_display2.h
kernel/.../disp/dev_disp.c
kernel/.../disp/dev_disp.h
kernel/.../disp/dev_fb.c
platform/.../libuapi/src/include/disp_dual.h
platform/.../libuapi/src/disp_dual.c
platform/.../libuapi/src/disp_mode_cli.c
platform/.../libuapi/src/Makefile
openwrt/package/allwinner/display/libuapi/Makefile
platform/.../tlayer_ctrl.c
platform/.../media_session.c
platform/.../media_session/daemon/Makefile
```
