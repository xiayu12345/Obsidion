---
project: LQ140M1JW61
kind: log
date: 2026-07-31
---

# LQ140M1JW61 GT9110H 触摸适配 — 开发记录

| 项 | 内容 |
|---|---|
| 日期 | 2026-07-31 |
| 结论 | **已定稿**：I2C 通；主机下发 1401598@1920×1080（186B）；四角 1:1 已验 |
| 状态 | 实机通过（无 LQ 门禁；原版 `SEND_CFG` 版本判断） |
| 镜像 | `out/h133_linux_p1_nor_LQ140M1JW61_uart0_nor.img` md5 `3222e8ce874bea9a69106ae8e2cf5a05` |

---

## 1. 一句话

主板 TP-H10 六线接 GT9110H；`ctp_name=gt9110h_lq140` → GROUP8（1401598，量程改 1920×1080）；`SEND_CFG=1` 按原 Goodix 规则（IC 配置版本 &lt;155 才写）。

---

## 2. 改动清单

| 文件 | 改动 |
|---|---|
| `gt9xx.h` | `GTP_DRIVER_SEND_CFG=1`；`CTP_CFG_GROUP8`←1401598 **有效 186B**（X/Y=1920/1080，chk=`0xFE`，Fresh=`1`） |
| `gt9xx.c` | `gt9110h_lq140`→sensor_id 7；`gtp_send_cfg` 写 `gtp_cfg_len+2`（勿写满 240） |
| LQ `board.dts` | `ctp_name=gt9110h_lq140`；`screen_max=1920×1080`；TWI1 / INT=PG3 / RST=PG2 / `0x5d` |

**控制流：** 与分支原版 `SEND_CFG=1` 一致——`opr_buf[0] < 155` 才下发，≥155 当 fixed 不写；无板级门禁。

---

## 3. 踩坑（必读）

| 现象 | 根因 | 处理 |
|---|---|---|
| 下发后 probe 成功但无坐标 | 供应商 `.cfg` 210B，有效配置止于 `0x8100`（**186B**）；按 210 算校验并 `write(240)` 把后续寄存器清 0 | GROUP8 只取 186B；写长度用 `cfg_len+ADDR` |
| 原 IC 量程 16384×9600 | 模组出厂 cfg | 主机改 X/Y 为 fb 分辨率后再下发 |
| `X_MAX` 日志有 1920 仍可能无点 | 日志来自驱动 RAM 缓冲，不等于 IC 已生效 | 以 `getevent` 四角 raw 为准 |

---

## 4. 硬件对照

| 项 | 值 |
|---|---|
| 芯片 | GT9110H（音乐骑士模组） |
| 接口 | I2C 六线（非 TB72C USB） |
| 主板 | TP-H10 JP13：RST/INT/SDA/SCK/GND/VCC → PG2/PG3/PG9/PG8 |
| 地址 | `0x5d`（备选 `0x14`） |
| 供应商 cfg | `硬件资料/音乐骑士/触摸资料/1401598-v1(25.2.26).cfg` |

---

## 5. 编译与验收

```bash
bash H133-AI-Skills/开发记录/板件开发记录/LQ140M1JW61/20260731-LQ140M1JW61-GT9110H触摸适配/patches/apply-lq140-gt9110h-tp.sh
./tools/build_p1_nor_LQ140M1JW61.sh kernel
# lunch 后 p 打镜像
```

```bash
dmesg | grep -iE 'gt9xx|X_MAX|Driver send|ctp-startup'
getevent -p /dev/input/event3
getevent -l /dev/input/event3   # 左上→右上→左下→右下
```

**已验四角 raw（约）：** (44,25) / (1897,31) / (32,1051) / (1888,1056) — 落在 1920×1080。  
desk 保持 `EVDEV_CALIBRATE=0`。

---

## 6. 参考

- `硬件资料/音乐骑士/触摸资料/1401598-v1(25.2.26).cfg`
- `硬件资料/音乐骑士/触摸资料/GT9110H&GT6108H编程指南_*.pdf`
- `TP驱动&鼠标&触摸/乐大海10.1寸TPC3386-GT911触摸驱动开发.md`
- 显示基线：[20260729 双显定稿](../20260729-LQ140M1JW61-板件新建与双显定稿/)

*2026-07-31 定稿：186B 下发 + 原版版本判断；实机 1:1 通过。*
