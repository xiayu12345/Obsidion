diff --git a/kernel/linux-5.4/drivers/video/fbdev/sunxi/disp2/disp/dev_disp.c b/kernel/linux-5.4/drivers/video/fbdev/sunxi/disp2/disp/dev_disp.c
index a76c3ac58..11e86e5b6 100644
--- a/kernel/linux-5.4/drivers/video/fbdev/sunxi/disp2/disp/dev_disp.c
+++ b/kernel/linux-5.4/drivers/video/fbdev/sunxi/disp2/disp/dev_disp.c
@@ -1147,8 +1147,8 @@ static struct work_struct g_dual_display_work;
 static struct workqueue_struct *g_dual_display_wq;
 static struct disp_layer_config g_dual_display_hdmi_layer;
 static bool g_dual_display_hdmi_layer_ready;
-/* true=同显把 WB 贴到 HDMI；false=异显腾出 DE1（capture/wq 不拆） */
-static bool g_dual_display_hdmi_output = true;
+/* 产品异显不走 WB；默认关 HDMI WB 层 */
+static bool g_dual_display_hdmi_output;
 
 #if IS_ENABLED(CONFIG_SUNXI_G2D) && IS_ENABLED(CONFIG_SUNXI_G2D_ROTATE)
 static g2d_blt_h g_dual_display_g2d_blt;
@@ -1781,8 +1781,8 @@ static int disp_dual_display_enable(void)
 	return -1;
 }
 
-/* 产品路径不用：切异显改走 set_hdmi_output。保留实现供极端排障。 */
-static int __maybe_unused disp_dual_display_disable(void)
+/* 拆 WB 管道；仅进程上下文（ioctl），勿在 HPD/irq 里调 */
+static int disp_dual_display_disable(void)
 {
 	struct disp_manager *mgr0, *mgr1;
 	struct disp_layer_config layer_config;
@@ -2535,12 +2535,9 @@ s32 disp_set_hdmi_detect(bool hpd)
 	    g_disp_drv.disp_init.b_init &&
 	    g_disp_drv.disp_init.output_type[1] == DISP_OUTPUT_TYPE_HDMI) {
 		disp_device_set_config(&g_disp_drv.disp_init, 1);
-		if (g_disp_drv.disp_init.chn_cfg_mode >= 1) {
-			msleep(600);
-			/* WB 管道常开；同/异显只切 HDMI 层显隐，不在此 disable */
-			disp_dual_display_enable();
-		} else if (!disp_dual_display_is_enabled() &&
-			   disp_fb_clone_is_allowed()) {
+		if (g_disp_drv.disp_init.chn_cfg_mode < 1 &&
+		    !disp_dual_display_is_enabled() &&
+		    disp_fb_clone_is_allowed()) {
 			msleep(300);
 			disp_fb_clone_to_screen(0, 1);
 		}
@@ -2704,14 +2701,7 @@ static void start_work(struct work_struct *work)
 		}
 	}
 
-	/* Auto-enable dual display when screen1 is active with dual-channel config */
-	if (g_disp_drv.disp_init.chn_cfg_mode >= 1
-	    && bsp_disp_get_output_type(1) != DISP_OUTPUT_TYPE_NONE) {
-		pr_info("[DISP] auto-enable dual display (chn_cfg=%d, screen1_output=%d)\n",
-			g_disp_drv.disp_init.chn_cfg_mode,
-			bsp_disp_get_output_type(1));
-		disp_dual_display_enable();
-	}
+	/* 产品异显不自动启 WB；HDMI 视频由 tlayer 直贴 */
 }
 
 static s32 start_process(void)
@@ -4774,14 +4764,11 @@ long disp_ioctl(struct file *file, unsigned int cmd, unsigned long arg)
 #endif
 
 	case DISP_DUAL_DISPLAY_ENABLE:
-		/* 确保 WB 管道起来；同↔异显勿用 DISABLE，改用 SET_HDMI_OUT */
 		ret = disp_dual_display_enable();
 		break;
 
 	case DISP_DUAL_DISPLAY_DISABLE:
-		/* 已废弃：拆 WB 会 scheduling while atomic 且产品改为管道常开 */
-		pr_warn("[DISP] DISP_DUAL_DISPLAY_DISABLE ignored; use SET_HDMI_OUT\n");
-		ret = -EINVAL;
+		ret = disp_dual_display_disable();
 		break;
 
 	case DISP_DUAL_DISPLAY_GET_EN:
diff --git a/kernel/linux-5.4/include/video/sunxi_display2.h b/kernel/linux-5.4/include/video/sunxi_display2.h
index cb5e54698..d84fee12d 100755
--- a/kernel/linux-5.4/include/video/sunxi_display2.h
+++ b/kernel/linux-5.4/include/video/sunxi_display2.h
@@ -981,9 +981,9 @@ enum tag_DISP_CMD {
 	DISP_EINK_OVERLAP_SKIP = 0x405,
 	DISP_EINK_UPDATE2 = 0x406,
 
-	/* MIPI+HDMI：WB 管道常开；切同/异显只用 SET_HDMI_OUT */
-	DISP_DUAL_DISPLAY_ENABLE = 0x407,       /* 确保 WB 起来 */
-	DISP_DUAL_DISPLAY_DISABLE = 0x408,      /* 废弃，内核直接 -EINVAL */
+	/* MIPI+HDMI 异显：产品不启 WB；DISABLE 拆管道 */
+	DISP_DUAL_DISPLAY_ENABLE = 0x407,       /* 调试：拉起 WB */
+	DISP_DUAL_DISPLAY_DISABLE = 0x408,      /* 拆 WB capture/wq */
 	DISP_DUAL_DISPLAY_GET_EN = 0x409,
 	DISP_FB_CLONE_ALLOW_SET = 0x40a,
 	DISP_FB_CLONE_ALLOW_GET = 0x40b,
diff --git a/platform/allwinner/display/libuapi/src/disp_dual.c b/platform/allwinner/display/libuapi/src/disp_dual.c
index 6919dd84a..1d0ac4d4c 100644
--- a/platform/allwinner/display/libuapi/src/disp_dual.c
+++ b/platform/allwinner/display/libuapi/src/disp_dual.c
@@ -40,10 +40,10 @@ static int disp_ioctl1(unsigned int cmd, unsigned long a0)
 	return ret;
 }
 
-/* 仅内部：确保 WB 已起来（幂等）；不对外提供 disable */
-static int ensure_wb_up(void)
+/* 产品路径：关掉 WB，不拉起 */
+static int ensure_wb_down(void)
 {
-	return disp_ioctl1(DISP_DUAL_DISPLAY_ENABLE, 0);
+	return disp_ioctl1(DISP_DUAL_DISPLAY_DISABLE, 0);
 }
 
 static int fb_clone_allow_off(void)
@@ -60,6 +60,11 @@ int sunxi_dual_display_is_enabled(void)
 	return ret ? 1 : 0;
 }
 
+int sunxi_dual_display_disable(void)
+{
+	return ensure_wb_down();
+}
+
 int sunxi_dual_display_set_hdmi_output(int on)
 {
 	return disp_ioctl1(DISP_DUAL_DISPLAY_SET_HDMI_OUT, on ? 1 : 0);
@@ -100,13 +105,12 @@ static int mode_load(sunxi_disp_mode_t *mode)
 	if (!mode)
 		return -EINVAL;
 
-	*mode = SUNXI_DISP_MODE_SAME;
+	*mode = SUNXI_DISP_MODE_DIFF;
 	fp = fopen(DISP_MODE_PATH, "r");
 	if (!fp)
 		return 0;
-	if (fscanf(fp, "%d", &v) == 1 &&
-	    (v == SUNXI_DISP_MODE_DIFF || v == SUNXI_DISP_MODE_DIFF_HDMI))
-		*mode = (sunxi_disp_mode_t)v;
+	if (fscanf(fp, "%d", &v) == 1 && v == SUNXI_DISP_MODE_DIFF_HDMI)
+		*mode = SUNXI_DISP_MODE_DIFF_HDMI;
 	fclose(fp);
 	return 0;
 }
@@ -154,13 +158,13 @@ int sunxi_disp_mode_apply(sunxi_disp_mode_t mode)
 	if (ret < 0)
 		return ret;
 
-	ret = ensure_wb_up();
+	ret = ensure_wb_down();
 	if (ret < 0 && ret != -EBUSY)
 		return ret;
 
-	/* SAME：HDMI 显示 WB；DIFF / DIFF_HDMI：关 HDMI WB 层，腾 DE1 */
-	ret = sunxi_dual_display_set_hdmi_output(
-		mode == SUNXI_DISP_MODE_SAME ? 1 : 0);
+	/* WB 已拆；再关 HDMI WB 层，避免残留 */
+	(void)mode;
+	ret = sunxi_dual_display_set_hdmi_output(0);
 	if (ret < 0)
 		return ret;
 	return 0;
@@ -170,8 +174,9 @@ int sunxi_disp_mode_set(sunxi_disp_mode_t mode)
 {
 	int ret;
 
-	if (mode != SUNXI_DISP_MODE_SAME &&
-	    mode != SUNXI_DISP_MODE_DIFF &&
+	if (mode == SUNXI_DISP_MODE_SAME)
+		mode = SUNXI_DISP_MODE_DIFF;
+	if (mode != SUNXI_DISP_MODE_DIFF &&
 	    mode != SUNXI_DISP_MODE_DIFF_HDMI)
 		return -EINVAL;
 	if (playback_busy())
@@ -185,7 +190,7 @@ int sunxi_disp_mode_set(sunxi_disp_mode_t mode)
 
 int sunxi_disp_mode_boot_sync(void)
 {
-	sunxi_disp_mode_t mode = SUNXI_DISP_MODE_SAME;
+	sunxi_disp_mode_t mode = SUNXI_DISP_MODE_DIFF;
 
 	mode_load(&mode);
 	return sunxi_disp_mode_apply(mode);
@@ -193,16 +198,14 @@ int sunxi_disp_mode_boot_sync(void)
 
 int sunxi_disp_video_screen_mask(void)
 {
-	sunxi_disp_mode_t mode = SUNXI_DISP_MODE_SAME;
+	sunxi_disp_mode_t mode = SUNXI_DISP_MODE_DIFF;
 
 	mode_load(&mode);
 	if (!sunxi_disp_hdmi_connected())
 		return 1;
-	if (mode == SUNXI_DISP_MODE_DIFF)
-		return 3;
 	if (mode == SUNXI_DISP_MODE_DIFF_HDMI)
 		return 2;
-	return 1;
+	return 3;
 }
 
 #else /* !__SUNXI_DISPLAY2__ */
@@ -211,6 +214,7 @@ int sunxi_disp_video_screen_mask(void)
 #include "disp_dual.h"
 
 int sunxi_dual_display_is_enabled(void) { return 0; }
+int sunxi_dual_display_disable(void) { return -ENOSYS; }
 int sunxi_dual_display_set_hdmi_output(int on) { (void)on; return -ENOSYS; }
 int sunxi_dual_display_get_hdmi_output(void) { return 0; }
 int sunxi_disp_hdmi_connected(void) { return 0; }
@@ -218,7 +222,7 @@ int sunxi_disp_mode_get(sunxi_disp_mode_t *mode)
 {
 	if (!mode)
 		return -EINVAL;
-	*mode = SUNXI_DISP_MODE_SAME;
+	*mode = SUNXI_DISP_MODE_DIFF;
 	return 0;
 }
 int sunxi_disp_mode_set(sunxi_disp_mode_t mode) { (void)mode; return -ENOSYS; }
diff --git a/platform/allwinner/display/libuapi/src/disp_mode_cli.c b/platform/allwinner/display/libuapi/src/disp_mode_cli.c
index 6f7f7c9b6..63f0db502 100644
--- a/platform/allwinner/display/libuapi/src/disp_mode_cli.c
+++ b/platform/allwinner/display/libuapi/src/disp_mode_cli.c
@@ -1,4 +1,4 @@
-/* 机上切换同/异显：disp_mode same|diff|diff_hdmi|get（WB 常开，只切 HDMI WB 层） */
+/* 机上切换异显：disp_mode diff|diff_hdmi|get（WB 关闭） */
 #include <stdio.h>
 #include <string.h>
 #include <errno.h>
@@ -6,22 +6,19 @@
 
 static const char *mode_name(sunxi_disp_mode_t mode)
 {
-	if (mode == SUNXI_DISP_MODE_DIFF)
-		return "diff";
 	if (mode == SUNXI_DISP_MODE_DIFF_HDMI)
 		return "diff_hdmi";
-	return "same";
+	return "diff";
 }
 
 static void usage(const char *argv0)
 {
 	fprintf(stderr,
-		"Usage: %s same|diff|diff_hdmi|get\n"
-		"  same       同显（WB→HDMI，视频贴 LCD）\n"
-		"  diff       异显（关 HDMI WB；播片时视频 LCD+HDMI；未插仍上 LCD）\n"
-		"  diff_hdmi  异显仅电视（关 HDMI WB；视频只贴 HDMI，LCD 仅 UI）\n"
+		"Usage: %s diff|diff_hdmi|get\n"
+		"  diff       异显（WB 关；播片时视频 LCD+HDMI；未插仍上 LCD）\n"
+		"  diff_hdmi  异显仅电视（WB 关；视频只贴 HDMI，LCD 仅 UI）\n"
 		"  get        查偏好 / HDMI / WB / hdmi_out / 视频屏\n"
-		"播中切换会失败（EBUSY）。切完需再播才会按新屏建层。\n",
+		"同显已取消；same 仍接受但按 diff。播中切换会失败（EBUSY）。切完需再播才会按新屏建层。\n",
 		argv0);
 }
 
@@ -36,7 +33,7 @@ int main(int argc, char **argv)
 	}
 
 	if (strcmp(argv[1], "get") == 0) {
-		mode = SUNXI_DISP_MODE_SAME;
+		mode = SUNXI_DISP_MODE_DIFF;
 		(void)sunxi_disp_mode_get(&mode);
 		printf("mode=%s hdmi=%d wb=%d hdmi_out=%d video_screen=%d\n",
 		       mode_name(mode),
diff --git a/platform/allwinner/display/libuapi/src/include/disp_dual.h b/platform/allwinner/display/libuapi/src/include/disp_dual.h
index 97e3745cf..f096adb43 100644
--- a/platform/allwinner/display/libuapi/src/include/disp_dual.h
+++ b/platform/allwinner/display/libuapi/src/include/disp_dual.h
@@ -1,12 +1,11 @@
 /*
- * MIPI+HDMI 同显/异显（libuapi）
+ * MIPI+HDMI 异显（libuapi）
  *
- * WB 管道由内核启动后常开；切模式只显隐 HDMI 上的 WB 层 + 视频贴屏：
- *   SAME：HDMI 显示 WB，视频 DE0
- *   DIFF：关 HDMI 上 WB 层（腾 DE1），视频同时贴 LCD+HDMI（须已插）
- *   DIFF_HDMI：关 HDMI 上 WB 层，视频只贴 HDMI（LCD 仅 UI；须已插）
+ * 产品无同显，WB 管道保持关闭。两种贴屏：
+ *   DIFF：视频同时贴 LCD+HDMI（须已插）——默认 / 设置页「LCD+HDMI」
+ *   DIFF_HDMI：视频只贴 HDMI（LCD 仅 UI；须已插）——设置页「仅HDMI」
+ *   SAME：枚举保留，set/get 一律按 DIFF
  * 播中勿切；未插 HDMI 时视频强制 LCD
- * SAME/DIFF 的 set/get 语义不变；设置页「异屏」仍是 DIFF
  */
 #ifndef __DISP_DUAL_H__
 #define __DISP_DUAL_H__
@@ -25,6 +24,7 @@ typedef enum {
 
 #ifndef DISP_DUAL_DISPLAY_ENABLE
 #define DISP_DUAL_DISPLAY_ENABLE       0x407
+#define DISP_DUAL_DISPLAY_DISABLE      0x408
 #define DISP_DUAL_DISPLAY_GET_EN       0x409
 #define DISP_FB_CLONE_ALLOW_SET        0x40a
 #define DISP_FB_CLONE_ALLOW_GET        0x40b
@@ -34,7 +34,8 @@ typedef enum {
 #define DISP_DUAL_DISPLAY_GET_HDMI_OUT 0x40d
 #endif
 
-/* HDMI 上 WB 层显隐（不拆 capture/wq） */
+/* 拆 WB；HDMI 上 WB 层显隐仅排障 */
+int sunxi_dual_display_disable(void);
 int sunxi_dual_display_set_hdmi_output(int on);
 int sunxi_dual_display_get_hdmi_output(void);
 int sunxi_dual_display_is_enabled(void);
@@ -46,7 +47,7 @@ int sunxi_disp_mode_set(sunxi_disp_mode_t mode);
 int sunxi_disp_mode_apply(sunxi_disp_mode_t mode);
 int sunxi_disp_mode_boot_sync(void);
 
-/* LayerCreate：未插 HDMI→1；DIFF+已插→3；DIFF_HDMI+已插→2；否则 1 */
+/* LayerCreate：未插 HDMI→1；DIFF+已插→3；DIFF_HDMI+已插→2 */
 int sunxi_disp_video_screen_mask(void);
 
 #ifdef __cplusplus
diff --git a/platform/thirdparty/gui/lvgl-8/app_depend/desk_huiyin/setting/core/desk_setting.c b/platform/thirdparty/gui/lvgl-8/app_depend/desk_huiyin/setting/core/desk_setting.c
index 7440292be..2bd106679 100644
--- a/platform/thirdparty/gui/lvgl-8/app_depend/desk_huiyin/setting/core/desk_setting.c
+++ b/platform/thirdparty/gui/lvgl-8/app_depend/desk_huiyin/setting/core/desk_setting.c
@@ -396,14 +396,18 @@ static void on_dd_disp_mode(lv_event_t *e)
     }
 
     id = lv_dropdown_get_selected(ui->screen_4_ddlist_5);
-    mode = (id == 1U) ? SUNXI_DISP_MODE_DIFF : SUNXI_DISP_MODE_SAME;
+    mode = (id == 1U) ? SUNXI_DISP_MODE_DIFF_HDMI : SUNXI_DISP_MODE_DIFF;
 
     ret = sunxi_disp_mode_set(mode);
     if (ret == 0) {
-        if (mode == SUNXI_DISP_MODE_DIFF && !sunxi_disp_hdmi_connected()) {
+        if (!sunxi_disp_hdmi_connected()) {
             lv_msgbox_create(NULL, "双屏模式",
-                "已切换为异屏。未检测到 HDMI，视频仍在 LCD 显示；"
-                "插线后请停止播放并重新点播。", btns_ok, true);
+                mode == SUNXI_DISP_MODE_DIFF_HDMI
+                    ? "已选仅 HDMI。未检测到 HDMI，视频仍在 LCD 显示；"
+                      "插线后请停止播放并重新点播。"
+                    : "已选 LCD+HDMI。未检测到 HDMI，视频仍在 LCD 显示；"
+                      "插线后请停止播放并重新点播。",
+                btns_ok, true);
         }
         return;
     }
@@ -412,7 +416,7 @@ static void on_dd_disp_mode(lv_event_t *e)
 
     if (ret == -EBUSY) {
         lv_msgbox_create(NULL, "双屏模式",
-            "请先停止播放后再切换同屏/异屏。", btns_ok, true);
+            "请先停止播放后再切换。", btns_ok, true);
         return;
     }
 
diff --git a/platform/thirdparty/gui/lvgl-8/app_depend/desk_huiyin/setting/core/desk_setting_image.c b/platform/thirdparty/gui/lvgl-8/app_depend/desk_huiyin/setting/core/desk_setting_image.c
index 50cdb87b3..2002dea16 100644
--- a/platform/thirdparty/gui/lvgl-8/app_depend/desk_huiyin/setting/core/desk_setting_image.c
+++ b/platform/thirdparty/gui/lvgl-8/app_depend/desk_huiyin/setting/core/desk_setting_image.c
@@ -49,11 +49,13 @@ void desk_setting_image_sync(desk_setting_ui_t *ui)
                                  (uint16_t)lv_get_sys_param(P_COLOR_TEMP));
     }
     if (ui->screen_4_ddlist_5) {
-        sunxi_disp_mode_t mode = SUNXI_DISP_MODE_SAME;
+        sunxi_disp_mode_t mode = SUNXI_DISP_MODE_DIFF;
 
         if (sunxi_disp_mode_get(&mode) == 0) {
             lv_dropdown_set_selected(ui->screen_4_ddlist_5,
-                (uint16_t)(mode == SUNXI_DISP_MODE_DIFF ? 1U : 0U));
+                (uint16_t)(mode == SUNXI_DISP_MODE_DIFF_HDMI ? 1U : 0U));
+        } else {
+            lv_dropdown_set_selected(ui->screen_4_ddlist_5, 0);
         }
     }
 
diff --git a/platform/thirdparty/gui/lvgl-8/app_depend/desk_huiyin/setting/ui/desk_setting_screen.c b/platform/thirdparty/gui/lvgl-8/app_depend/desk_huiyin/setting/ui/desk_setting_screen.c
index 944062622..d3ff8fa3c 100644
--- a/platform/thirdparty/gui/lvgl-8/app_depend/desk_huiyin/setting/ui/desk_setting_screen.c
+++ b/platform/thirdparty/gui/lvgl-8/app_depend/desk_huiyin/setting/ui/desk_setting_screen.c
@@ -214,7 +214,7 @@ void desk_setting_screen_create(desk_setting_ui_t *ui, lv_obj_t *parent_cont)
 
     //Write codes screen_4_ddlist_5
     ui->screen_4_ddlist_5 = lv_dropdown_create(ui->screen_4_cont_16);
-    lv_dropdown_set_options(ui->screen_4_ddlist_5, "同屏\n异屏");
+    lv_dropdown_set_options(ui->screen_4_ddlist_5, "LCD+HDMI\n仅HDMI");
     lv_obj_set_pos(ui->screen_4_ddlist_5, 602, 15);
     lv_obj_set_size(ui->screen_4_ddlist_5, 174, 39);
 
diff --git a/platform/thirdparty/gui/lvgl-8/lib/media_session/src/pro/ktv_player_ui.c b/platform/thirdparty/gui/lvgl-8/lib/media_session/src/pro/ktv_player_ui.c
index a922da3c6..8057f1cd7 100644
--- a/platform/thirdparty/gui/lvgl-8/lib/media_session/src/pro/ktv_player_ui.c
+++ b/platform/thirdparty/gui/lvgl-8/lib/media_session/src/pro/ktv_player_ui.c
@@ -272,7 +272,7 @@ static void ktv_player_ui_apply_cached_zorder(void)
 }
 
 /* 板端后路：setting.ini [display] video_rotate=0..3；每次 apply 重读，与 UI disp_rotate 独立。
- * 异显视频只贴 HDMI：ini 的 180 会倒着，强制 0；同显仍跟 ini。 */
+ * 异显视频上 HDMI：ini 的 180 会倒着，强制 0。 */
 static int ktv_player_ui_tplayer_rotate_degree(void)
 {
     static const int deg_map[4] = {
@@ -282,9 +282,11 @@ static int ktv_player_ui_tplayer_rotate_degree(void)
         TPLAYER_VIDEO_ROTATE_DEGREE_270,
     };
     lv_video_rect_config_t cfg;
-    sunxi_disp_mode_t mode = SUNXI_DISP_MODE_SAME;
+    sunxi_disp_mode_t mode = SUNXI_DISP_MODE_DIFF;
 
-    if (sunxi_disp_mode_get(&mode) == 0 && mode == SUNXI_DISP_MODE_DIFF) {
+    if (sunxi_disp_mode_get(&mode) == 0 &&
+        (mode == SUNXI_DISP_MODE_DIFF ||
+         mode == SUNXI_DISP_MODE_DIFF_HDMI)) {
         return TPLAYER_VIDEO_ROTATE_DEGREE_0;
     }
     if (lv_video_rect_config_load(&cfg) != 0) {
