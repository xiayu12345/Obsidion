# 显示测试

## 测试范围

| 项目 | 说明 |
|---|---|
| LCD | 点屏、分辨率、方向、背光 |
| HDMI | 插拔、分辨率、热插拔、音视频输出 |
| 双显 | LCD + HDMI 同显/异显 |
| 旋转 | LVGL 软旋转、视频旋转 |
| 视频层/UI 层 | TPlayer 视频与 LVGL UI 共存 |

## 测试记录

| 日期 | 测试项 | 输出设备 | 镜像/应用版本 | 结果 | 记录 |
|---|---|---|---|---|---|
| `[待填写]` | `[待填写]` | `[待填写]` | `[待填写]` | `[待填写]` | `[待填写]` |

## 常用命令

```bash
adb shell "cat /sys/class/disp/disp/attr/sys"
adb shell "cat /sys/class/disp/disp/attr/sys | grep -A2 'screen 1'"
adb shell dmesg | grep -Ei "disp|hdmi|lcd|mipi|layer|g2d"
```

## 记录模板

```text
日期：
测试人：
LCD 状态：
HDMI 状态：
分辨率：
旋转角度：
视频源：
UI 状态：
实际现象：
结论：
遗留问题：
```
