# 应用测试

## 测试范围

| 项目 | 说明 |
|---|---|
| `lv_projector` 启动 | 应用启动、崩溃、日志 |
| UI 页面 | 页面切换、控件状态、文字显示 |
| 输入 | 触摸、鼠标、按键 |
| 播放 | 本地视频、HDMI 输出、音频 |
| 网络 | WiFi、SFTP、ADB 部署 |
| 稳定性 | 长时间运行、反复切换、异常恢复 |

## 测试记录

| 日期 | 测试项 | 应用版本 | 镜像版本 | 结果 | 记录 |
|---|---|---|---|---|---|
| `[待填写]` | `[待填写]` | `[待填写]` | `[待填写]` | `[待填写]` | `[待填写]` |

## 常用命令

```bash
adb push ./lv_projector /usr/bin/lv_projector
adb shell "chmod 777 /usr/bin/lv_projector && /usr/bin/lv_projector"
adb shell logread -f
adb shell dmesg | tail -100
```

## 记录模板

```text
日期：
测试人：
应用版本：
镜像版本：
测试页面/功能：
操作步骤：
预期结果：
实际结果：
日志：
结论：
遗留问题：
```
