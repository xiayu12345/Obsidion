---
title: "热敏打印与 MQTT 开发记录（BK7252N）"
description: ""
updated: 1780371367054
created: 1780371330023
---

# 热敏打印与 MQTT 开发记录（BK7252N）


# 热敏打印与 MQTT 开发记录（BK7252N）

**记录日期**：?2026-05-03  
**工程**：`bk7252n_sdk-SDK_3.0.76.1_witch_lcd`（BK7252N + RT-Thread）?  
**说明**：与历史文档目录?? `0wjz-docs` 并列；本仓文件集中汇总本仓?已落地改动、用法与风险?

---

## 1. 编译命令

```bash
export RTT_EXEC_PATH=<gcc-arm-none-eabi-5_4-2016q3>/bin/
scons --beken=bk7252n -j8
```

产物?录：`out/`（含 `rtthread.bin`、`all_4M.*.bin` 等）?

---

## 2. `prn_tune`：白行跳过可关闭

**背景**：位图打印中连续空白行可走「批量快进」分?；二维码、大块留白等场景对?间节?敏感时希望关闭?

**实现**：`prn_tune set white` 允?? **`0..512`**?**`0` = 禁用**白?连?跳过（逻辑条件? `white_run_max > 0` 才进入?分?）?

**涉及文件**：`applications/Printer/thermal_printer.c`（校验与?助文案）?

**用法示例**?

```text
prn_tune set white 0
```

---

## 3. GPIO36 长按二维码打印（走当? `prn_tune`?

**行为**?**长按 P36**（与 `multi_button` ? 1s 长按阈值一致）创建线程 **`qr_prn`**，在其中打印静态位? **`QR_code_data`**?384×384），?选一行?明文字?**不再调用 `thermal_printer_set_speed_level`**，整段使用当? **`prn_tune` / 运?时** 参数?

**默??明?**：宏 **`BUTTON_QR_PRINT_CAPTION`**，默认字符串 **`1234567890`**（可? `button_control.c` ?改或编译 `-D` 覆盖）?

**注意**：P37 仍为长按录音 / 二级桌面逻辑；P36 ?按仍为?面切换等原有逻辑?

**涉及文件**?

- `applications/button_control/button_control.c`
- `applications/Printer/thermal_printer.c`（`QR_code_printer`?
- `applications/Printer/teermal_print.c`（`thermal_printer_print_text` 去掉强制 `set_speed_level`?
- `applications/Printer/thermal_printer.h`

---

## 4. MQTT：`imagePrint` ? `result.start` 共用接收大缓?

**?题现?**：`result.start` 打印成功后，`imagePrint` 易出? **`mqtt backend alloc print buffer failed`**。根因是 `imagePrint` 在独? `rt_malloc` 收满后，`spawn_print_thread` 又?非 `g_binary_shared_buf` 指针 **再申请同等大小副?**，短时堆占用翻倍，易失败?

**??**：`imagePrint` 接收缓冲改为? **`result.start`** 相同使用 **`g_binary_shared_buf`**（?量? **`MQTT_BINARY_SHARED_BUF_SIZE`**，?? 20KiB）；`spawn_print_thread` 走「零拷贝」分??**`mqtt_backend_image_print_rx_reset`** 在指针等于共?缓冲? **不得 `rt_free`**?

**约束**：单? **`totalSize` ? `MQTT_BINARY_SHARED_BUF_SIZE`**?

**共性?明**：与 `result` 链路相同，共?缓冲在打印未完成时若又开始下一会话，理论上存在覆盖风险；业务上宜避免并发重叠?

**涉及文件**：`applications/mqtt_backend/mqtt_backend_binary_rx.c`

---

## 5. `prn_tune preset devterm`（?齐 ClockworkPi DevTerm 开源思路?

**意图**：将 DevTerm `config.h` ? **`HEAT_TIME=100`** ? `printer.c` **`print_dots_8bit`** ? **`d` 次加?、每? `(100 + d×46)` µs、脉? 14µs** 的理念，映射到本仓固件? **`whole` + `whole_line_strobe_pulses`**?

**命令**?

```text
prn_tune preset devterm       ; 默?? d=6
prn_tune preset devterm 8     ; d=1..15，类比开? DC2 # n 浓度
prn_tune show
```

**预??点**：`heat_total = d × (100 + d×46)`?5?/25?/40? **同一基准**（开源打印路径不按温调脉宽）?**`heat_lim`** 放?下限以便短脉?不?默?? 760µs 顶???**`dense`/白?合?/gshape/linegap 分??**等关?或拉平；**`motor_step_us=880`**（?应开? `acc_time[]` ??量级）；**`speed_level=4`**（避开? 5 下??? +40µs）?

**涉及文件**：`applications/Printer/thermal_printer.c`（`preset_open_source_devterm`?

---

## 6. 参数使用梳理（简要）

- **`preset`**：整包基底（`L1..L5`、`cur5`、`prev5`、`devterm`）；之后? **`set`** 会局部?盖；再? **`preset`** 会整体重写?
- **`set`**：分项调节电机、?间、加?、`heatmode`/`heatpulse`、白行、`linegap_*`、`dense`、`stop`/`resume` 等?
- **`show`**：核对当前生效值?
- **`whole` + `heatpulse`**：整行加?与?脉冲拆分时序；**`gshape` ? `seg` 生效**?

更细的逐项说明见仓库内 `0wjz-docs/打印机参数排列组合?录.md` ? [prn_tune串口调参命令](../%E6%8C%87%E5%8D%97/prn_tune%E4%B8%B2%E5%8F%A3%E8%B0%83%E5%8F%82%E5%91%BD%E4%BB%A4.md)?

---

## 7. ?件与安全警示（重要）

现场反?：**按开?/DevTerm 思路高能量打印曾导致多台打印头或驱动损坏**?

DevTerm 参数针?? **特定头型、供电与散热**；在??件上 **µs 加热时间与实际功率并非一一对应**。使? **`preset devterm` 或大 `d`、高 duty、连?大图/实心二维?** 前务必：

- 从小 **`d`**、长 **`motor`** / **`line`**、保? **`heatlim`** 起?，?时打印摸温；
- 避免长时间连?满负荷；
- 核?供电、STROBE 驱动与头规格?否与评估一致；
- 温度采样? **`stop`/`resume`** 必须?信?

**建??**：量产或长期验证? **`L1` 类保守档 + 实测升温曲线** 为主?**`devterm` 仅作对比实验**，不宜默认出厂?

---

## 8. 参考路径索?

| 内?? | ?? |
|------|------|
| 历史同类记录 | `0wjz-docs/开发?录/2026-05-01_0234-AI打印机固件开发?录.md` |
| 参数组合手?? | `0wjz-docs/打印机参数排列组合?录.md` |
| 串口调参 | [prn_tune串口调参命令](../%E6%8C%87%E5%8D%97/prn_tune%E4%B8%B2%E5%8F%A3%E8%B0%83%E5%8F%82%E5%91%BD%E4%BB%A4.md) |
| DevTerm 源码?? | `0wjz-docs/开源打印机代码/DevTerm/Code/thermal_printer/` |

---

## 9. 变更文件清单（本仓?汇总）

| 文件 | 摘?? |
|------|------|
| `applications/Printer/thermal_printer.c` | `white 0..512`；`QR_code_printer`；`preset devterm` |
| `applications/Printer/thermal_printer.h` | `QR_code_printer` 声明 |
| `applications/Printer/teermal_print.c` | 文本仓打印不强制改 speed |
| `applications/button_control/button_control.c` | P36 长按 `qr_prn` 线程 |
| `applications/mqtt_backend/mqtt_backend_binary_rx.c` | `imagePrint` 共用共享缓冲 |

---

*?文档目录随仓库迭代；若目录策略统一为仅使用 `0wjz-docs`，可将本仓文件复制或迁移至 `0wjz-docs/开发?录/` 并保持单一来源?*
